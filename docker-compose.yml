services:
  jupyter:
    image: jupyter/scipy-notebook:latest
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    command: >
      bash -c "pip install schemdraw --quiet &&
               start-notebook.sh --NotebookApp.token='' --NotebookApp.password=''"
